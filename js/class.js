export default class Box {
    constructor(id, level, boxType, xBox, yBox, L, onSpot) {
        this.id = id
        this.level = level
        this.boxType = boxType
        this.xBox = xBox
        this.yBox = yBox
        this.L = L
        this.onSpot = onSpot
    }
}